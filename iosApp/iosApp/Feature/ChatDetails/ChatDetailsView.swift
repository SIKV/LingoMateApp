import SwiftUI

struct ChatDetailsView: View {
    var body: some View {
        VStack {
            Text("Chat Details")
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .center)
    }
}
