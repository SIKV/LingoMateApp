import SwiftUI

struct MoreView: View {
    var body: some View {
        VStack {
            Text("More")
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .center)
    }
}
